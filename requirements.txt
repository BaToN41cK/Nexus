# ======================================================================
# Nexus — полная установка одной командой
#
#   pip install -r requirements.txt
#
# Эта команда устанавливает ВСЕ зависимости (core + optional):
#   groq, openai, anthropic, ollama, MCP, RAG, Chroma, interactive UI
# ======================================================================

# --- Core (всегда нужны) ---
groq>=1.0.0
requests>=2.32.5
beautifulsoup4
python-dotenv
pyyaml
rich
pygments
youtube-transcript-api
pypdf
python-docx
python-pptx
openpyxl

# --- LLM-провайдеры (опционально — раскомментируй нужные, или оставь всё) ---
openai
anthropic
ollama

# --- MCP сервер (для IDE-интеграции) ---
mcp

# --- RAG (Retrieval-Augmented Generation) ---
sentence-transformers>=5.1.2,<6.0
faiss-cpu>=1.13.0,<2.0

# --- ChromaDB (альтернатива FAISS) ---
chromadb>=1.5.9,<2.0

# --- Интерактивный UI (prompt_toolkit) ---
prompt-toolkit

# --- Поисковые бэкенды ---
# Tavily (требует TAVILY_API_KEY)
# tavily-python
# Bing Search API (требует BING_API_KEY)
# DuckDuckGo — встроен, никаких доп. зависимостей не нужно
# SearXNG — REST API, никаких доп. зависимостей не нужно